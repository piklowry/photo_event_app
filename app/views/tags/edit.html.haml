= form_for @tag, url: "/tags/#{@tag.id}" do |f|
	= render "form", f: f
	= f.submit "EDIT"



= link_to "DELETE",  {:action => "destroy", :id => "#{@tag.id}"}, :method => :delete, :class => "icon-delete", :confirm => "Are you sure"